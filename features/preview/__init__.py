"""Cards preview — the deck's whole card list as a flippable grid.

`content` and `grid` never import `aqt`, so `tools/test_preview.py` can load
them directly; `screens/preview.py` is where anything needing Anki belongs.
"""
